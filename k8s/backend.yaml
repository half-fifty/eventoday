apiVersion: apps/v1
kind: Deployment
metadata:
  name: eventoday-be
  namespace: eventoday
  labels:
    app: eventoday-be
spec:
  replicas: 1
  selector:
    matchLabels:
      app: eventoday-be
  template:
    metadata:
      labels:
        app: eventoday-be
    spec:
      imagePullSecrets:
        - name: ghcr-pull-secret
      containers:
        - name: eventoday-be
          image: ghcr.io/half-fifty/eventoday-be:e9c351b95aef4561b8a24c92243b14b52b80b7b2
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          resources:
            requests:
              cpu: 250m
              memory: 384Mi
            limits:
              memory: 768Mi
          startupProbe:
            tcpSocket:
              port: 8080
            failureThreshold: 30
            periodSeconds: 5
          readinessProbe:
            tcpSocket:
              port: 8080
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: 8080
            periodSeconds: 15
          env:
            - name: TZ
              value: "Asia/Seoul"
            - name: DB_URL
              value: "jdbc:postgresql://10.0.0.31:5432/expo_platform"
            - name: REDIS_HOST
              value: "10.0.0.31"
            - name: REDIS_PORT
              value: "6379"
            - name: FRONTEND_URL
              value: "https://eventoday.duckdns.org"
            - name: COOKIE_SECURE
              value: "true"
            - name: ELASTICSEARCH_URIS
              value: "http://eventoday-elasticsearch:9200"
          envFrom:
            - secretRef:
                name: eventoday-secret
---
apiVersion: v1
kind: Service
metadata:
  name: eventoday-be
  namespace: eventoday
spec:
  selector:
    app: eventoday-be
  ports:
    - port: 8080
      targetPort: 8080
